{
  "server_name": "agile-hospital",
  "tools": [
    {
      "name": "get_patient_age",
      "description": "Return the age of a patient in years given their hospital ID.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "patient_id": {
            "type": "string",
            "description": "the patient's numeric hospital identifier (e.g. \"45\")"
          }
        },
        "required": [
          "patient_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "get_patient_status",
      "description": "Return the current admission status of a patient.\n\nIf dateEnd is None the patient is currently admitted.\nIf dateEnd is set the patient has been discharged.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "patient_id": {
            "type": "string",
            "description": "the patient's numeric hospital identifier (e.g. \"45\")"
          }
        },
        "required": [
          "patient_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "get_admission_history",
      "description": "Return the admission record(s) for a patient.\n\nNOTE: Agile's current API returns one event per request.\nMulti-event history requires a dedicated history endpoint (Phase 2).",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "patient_id": {
            "type": "string",
            "description": "the patient's numeric hospital identifier (e.g. \"45\")"
          }
        },
        "required": [
          "patient_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "get_patient_summary",
      "description": "Generate a concise clinical summary for a patient using an LLM.\n\nCalls the LLM N times independently and returns the most consistent\nresponse together with a semantic confidence score (0\u20131).\nLOW confidence means the model was uncertain \u2014 clinician must verify.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "patient_id": {
            "type": "string",
            "description": "numeric hospital identifier (e.g. \"45\")"
          }
        },
        "required": [
          "patient_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "get_patient_discharge_draft",
      "description": "Generate a draft discharge letter section for a patient.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "patient_id": {
            "type": "string",
            "description": "numeric hospital identifier (e.g. \"45\")"
          }
        },
        "required": [
          "patient_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "get_patients_by_diagnosis",
      "description": "Return all patients whose primary diagnosis code starts with the given prefix.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "diagnosis_code_prefix": {
            "type": "string",
            "description": "numeric diagnosis-code prefix (e.g. \"428\" for heart failure, \"434\" for stroke)"
          }
        },
        "required": [
          "diagnosis_code_prefix"
        ],
        "type": "object"
      }
    },
    {
      "name": "get_cohort_summary",
      "description": "Generate a cohort-level summary with per-patient LLM summaries\nand a gated aggregate confidence score.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "diagnosis_code_prefix": {
            "type": "string",
            "description": "numeric diagnosis-code prefix (e.g. \"428\", \"434\")"
          }
        },
        "required": [
          "diagnosis_code_prefix"
        ],
        "type": "object"
      }
    },
    {
      "name": "get_recently_admitted",
      "description": "Return all patients admitted within the last N days.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "days": {
            "default": 7,
            "type": "integer",
            "description": "look-back window in days (default: 7)"
          }
        },
        "type": "object"
      }
    },
    {
      "name": "evaluate_clinical_output_guardrail",
      "description": "Evaluate whether a generated clinical output should be accepted, reviewed,\nor rejected.\n\nThis tool does not access patient data and does not call the LLM. It only\napplies the uncertainty-aware guardrail policy to a task type and confidence\nscore. The confidence value is a semantic self-consistency score from\nrepeated LLM sampling; it is not a guarantee of clinical truth.",
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "task_type": {
            "type": "string",
            "description": "Clinical task category, e.g. patient_summary,\ndischarge_draft, medication_or_therapy, administrative_summary."
          },
          "confidence": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Optional semantic self-consistency score in [0, 1]."
          }
        },
        "required": [
          "task_type"
        ],
        "type": "object"
      }
    }
  ]
}